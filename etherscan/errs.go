package main

var (
	// Error
	ReadBodyErr     string = "Read body error"
	InvalidJson     string = "invalid json"
	LackAccountInfo string = "lack account info"
	LackTargetInfo  string = "lack target info"
	GetTxListErr    string = "get txlist err"

	// Ok
	MatchAcccountTx string = "match account tx"
)
